{
  "name": "swap-to-local-color-variables",
  "version": "1.0.0",
  "description": "Reassign layers bounded to external color variables to local ones of the same name in a matter of seconds.",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.58.0",
    "typescript": "*"
  }
}
